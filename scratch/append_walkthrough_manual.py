import os

walkthrough_path = r"C:\Users\Vitaly\.gemini\antigravity\brain\bf56e7e7-5c62-460d-a251-403b9cc6776f\walkthrough.md"

walkthrough_text = """
### 15. Интеграция панели ручного нагрева с калькулятором расходов (New)
- **Интерактивная Lovelace-панель в ручном режиме (`boiler-card.js v1.6.0`)**:
  - Создана новая секция управления ручным нагревом, отображаемая только при переключении системы в режим `Manual`.
  - Добавлен выпадающий список выбора режима нагрева:
    - `GAS - gas only`
    - `GAS_PUMP - Gas + pump`
    - `ELEC - Electro only`
    - `Elec_pump - eclectro + pump`
  - Реализовано мгновенное переключение байпаса: выбор `GAS` отправляет команду на закрытие байпаса (`OFF`), выбор любого другого режима автоматически открывает байпас (`ON`).
  - Добавлен ползунок выбора целевого сетпоинта температуры с автоматической корректировкой границ: минимум равен `t_min` (уставке термостата), максимум динамически переключается на `t_max_gas` (для газовых режимов) или `t_max_elec` (для электрических).
  - При перемещении ползунка отображается прогнозируемый расход ресурсов для нагрева до выбранного уровня:
    - Для газовых режимов: "Расход газа: X.XX м³"
    - Для электрических режимов: "Расход энергии: Y.YY кВт"
    - Расчет производится на основе калибровочных данных из `sensor.boiler_calibration` с автоматическим фолбэком (80.0 °C/м³ для газа и 8.6 °C/кВтч для электричества).
- **Бэкенд автоматизация цикла нагрева (`boiler_controller.py`)**:
  - Реализованы сервисы `ems.start_manual_heating` и `ems.stop_manual_heating`, также задокументированные в `services.yaml`.
  - Метод `_async_apply_manual_heating(self)` циклически опрашивает температуры и применяет алгоритм:
    - **GAS**: байпас закрыт, греем газом до сетпоинта.
    - **GAS_PUMP**: байпас открыт, греем газом до сетпоинта с постоянной работой насоса перекачки.
    - **ELEC**: байпас открыт, греем ТЭНом до сетпоинта.
    - **ELEC_PUMP**: байпас открыт, греем ТЭНом до сетпоинта с периодической перекачкой (pump dump) в газовый котел (включение насоса при `setpoint - 5.0`°C, выключение при `setpoint - 10.0`°C).
  - Нагрев автоматически отключается по завершении (при `temp >= setpoint`), а также принудительно гасится при потере связи с температурными датчиками (`Fail-Safe`).
- **Синхронизация состояний**:
  - Состояние ручного нагрева передается через атрибуты `sensor.boiler_dp` (`manual_heating_active`, `manual_heating_mode`, `manual_heating_setpoint`).
  - Прослушивание события `ems_manual_heating_updated` в `sensor.py` гарантирует мгновенный рендеринг состояния и блокировку/разблокировку органов управления в карточке Lovelace.
"""

with open(walkthrough_path, "r", encoding="utf-8") as f:
    content = f.read()

new_content = content.strip() + "\n" + walkthrough_text

with open(walkthrough_path, "w", encoding="utf-8") as f:
    f.write(new_content)

print("Walkthrough appended successfully.")
