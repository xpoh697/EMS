# -*- coding: utf-8 -*-
import os

debate_path = r"e:\HA_INTEGRATIONS\EMS\DEBATE.md"

new_debate = """
## [2026-05-26 21:15] Задача: Разделение PV-лимитов на сегодня/завтра и вывод их в секцию температур с остатком на сегодня

### Archi
Пользователь хочет улучшить юзабилити и точность отображения лимитов свободной PV-энергии:
1. **Математическое разделение лимитов в `boiler_dp_engine.py`**:
   - Определяем текущую дату как дату первого слота: `today_date = slots[0].get("date") if slots else None`.
   - Разделяем расчет излишков: `curtailed_pv_today` накапливает утилизируемую PV-энергию для слотов с датой `today_date`, а `curtailed_pv_tomorrow` — для всех последующих слотов.
   - Разделяем распределение свободного бюджета (арбитраж): свободные слоты для сегодняшнего лимита выделяются только в пределах сегодняшних слотов, а для завтрашнего лимита — только в пределах завтрашних слотов.
   - Рассчитываем раздельные уставки температур `dynamic_t_max_elec_today` и `dynamic_t_max_elec_tomorrow`.
   - В переходах DP используем соответствующую уставку в зависимости от того, к какому дню относится слот.
   - Считаем плановое потребление электробойлера сегодня: `today_planned_elec = sum(step["energy"] for step in best_path if step["date"] == today_date and step["mode"] in ("ELEC", "ELEC_PUMP"))`.
   - Вычисляем остаток лимита на сегодня: `remaining_pv_today = max(0.0, curtailed_pv_today - today_planned_elec)`.
   - Добавляем новые метрики в словарь `stats`: `curtailed_pv_today`, `curtailed_pv_tomorrow`, `remaining_pv_today`.
2. **Отображение в UI в `boiler-card.js`**:
   - Добавляем в `_buildSkeleton()` после строки температуры подачи (`houseFlowRow`) новые строки: `Свободный PV-лимит сегодня` (`this._pvLimitTodayVal`) и `Свободный PV-лимит завтра` (`this._pvLimitTomorrowVal`).
   - В методе `_updateValues()` считываем новые значения из `dpS?.attributes?.stats`.
   - Форматируем вывод: для сегодняшнего лимита выводим `X.X кВтч (осталось: Y.Y кВтч)`. Если лимит равен 0.0, скрываем соответствующую строку, чтобы не перегружать интерфейс.
   - Убираем отображение лимита из плашки `this._autoHint` в подвале, оставляя там только надпись `Автоуправление` (без дублирования).
3. **Версионирование в `const.py`**:
   - Повышаем версию до `0.3.13`.

### Skeptic
Критический анализ решения Archi:
1. **Обработка пустых или некорректных данных на фронтенде**:
   При первой инициализации карточки или при перезагрузке HA атрибуты `dpS.attributes.stats` могут временно отсутствовать или быть пустыми. В JS коде обращение к `toFixed(1)` на `undefined` вызовет ошибку и заблокирует рендеринг всей карточки. Необходимо добавить защитную проверку `(val !== undefined && val !== null)` перед форматированием.
2. **Динамический лимит ТЭНа в переходах**:
   Если `eff_elec_pump` равен нулю (например, из-за некорректных калибровочных данных), то формула расчета `budget_rise` может вызвать деление на ноль или дать непредсказуемый результат. Нужно гарантировать безопасный расчет `budget_rise` с проверкой `eff_elec_pump > 0.0` и инициализацией дефолтного лимита.
3. **Совместимость со старыми карточками и сброс кеша**:
   Поскольку мы переносим PV-лимит из подвала в секцию температур и меняем JS файл карточки, Home Assistant может использовать старый закэшированный `boiler-card.js`. Необходимо инкрементировать версию `CARD_VERSION` в коде карточки до `1.9.0`, чтобы HA принудительно обновил кэш фронтенда.

### Archi (Итерация 2)
Критика полностью обоснована и принята. Вносим следующие защитные меры:
1. В `boiler-card.js` повышаем константу `CARD_VERSION` до `"1.9.0"`.
2. Добавляем строгие проверки на существование и тип данных в JS перед вызовом `.toFixed(1)`:
   `const val = dpS?.attributes?.stats?.curtailed_pv_today;`
   `if (typeof val === 'number') { ... }`
3. В `boiler_dp_engine.py` гарантируем безопасность вычисления `budget_rise`:
   `budget_rise = curtailed_pv_today * eff_elec_pump if eff_elec_pump > 0.0 else 0.0`
   Это полностью исключает ошибки деления или умножения на некорректные калибровочные данные.

### Skeptic (Итерация 2)
Доработки полностью закрывают риски безопасности типов и производительности. Использование константной фильтрации на фронтенде и строгих проверок на бэкенде гарантирует стабильность системы при любых перезапусках HA. Согласовано.

### Заключение
Согласовано:
1. В `boiler_dp_engine.py` разделить расчет и распределение лимитов PV-генерации на сегодня и на завтра. Рассчитать остаток сегодняшнего лимита `remaining_pv_today`.
2. В `boiler-card.js` повысить версию до `1.9.0`, убрать вывод лимита из `_autoHint`, добавить строки `_pvLimitTodayVal` и `_pvLimitTomorrowVal` в блок температур и выводить в них данные с безопасной валидацией типов.
3. Повысить версию интеграции в `const.py` до `0.3.13`.
"""

with open(debate_path, "a", encoding="utf-8") as f:
    f.write(new_debate)

print("Debate appended successfully.")
