import os

walkthrough_path = r"C:\Users\Vitaly\.gemini\antigravity\brain\bf56e7e7-5c62-460d-a251-403b9cc6776f\walkthrough.md"

walkthrough_text = """
### 14. Программный термостат отсечки нагрева и динамический сброс тепла (Pump Dump) (New)
- **Программный термостат отсечки нагрева**:
  - Реализованы флаги программной отсечки `self._elec_cutoff_active` и `self._gas_cutoff_active` в `BoilerController` (`boiler_controller.py`).
  - ТЭН и газовый котел автоматически отключаются при достижении целевых максимальных настроек (`elec_boiler_max_temp` для электрического, `gas_boiler_max_temp` для газового).
  - Добавлен температурный гистерезис **2.0°C** для ТЭНа и газа для исключения тактования (быстрого включения/выключения).
- **Динамический сброс тепла (Pump Dump)**:
  - Реализован алгоритм контроля перегрева в режиме `ELEC`. Если температура ТЭНа поднимается до **`setpoint - 5.0`°C** (например, 65°C), автоматически запускается циркуляционный насос и открывается байпасный клапан, перекачивая тепло в систему.
  - Насос выключается при охлаждении бака ниже **`setpoint - 10.0`°C** (например, 60°C).
- **Физическая логика байпасного клапана**:
  - Байпас принудительно закрывается (`OFF`) строго в режиме `"GAS"`.
  - В режимах нагрева (`ELEC`, `ELEC_PUMP`, `GAS_PUMP`) байпас принудительно открывается (`ON`).
  - В режиме `"IDLE"` состояние байпаса сохраняется неизменным, исключая лишние переключения.
- **Fail-Safe защита**:
  - При пропадании связи с температурными датчиками (переход в `None`, `unknown` или `unavailable`) во время активного нагрева в режиме `Auto`, нагреватели ТЭН и газ немедленно и безопасно выключаются.
- **Защита от ручного переключения (Override Check)**:
  - Метод `_async_override_check` обновлен для учета флагов температурной отсечки и динамической перекачки тепла, что полностью устраняет ложные срабатывания защиты и предотвращает циклы откатов.
"""

with open(walkthrough_path, "r", encoding="utf-8") as f:
    content = f.read()

new_content = content.strip() + "\n" + walkthrough_text

with open(walkthrough_path, "w", encoding="utf-8") as f:
    f.write(new_content)

print("Walkthrough appended successfully.")
