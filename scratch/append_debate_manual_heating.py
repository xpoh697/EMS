import os

debate_path = r"E:\HA_INTEGRATIONS\EMS\DEBATE.md"

debate_text = """
## [2026-05-23 17:43] Задача: Интеграция ручного цикла нагрева (режимы GAS, GAS_PUMP, ELEC, ELEC_PUMP) с интерфейсом выбора режима, ползунком сетпоинта, прогнозом затрат и кнопкой старт/стоп в Lovelace-карточку.

### Archi
Предлагаю комплексную интеграцию ручного режима нагрева в бэкенд и Lovelace-карточку:
1. **Бэкенд (`boiler_controller.py`)**:
   - Добавить переменные состояния: `self._manual_heating_active = False`, `self._manual_heating_mode = "GAS"`, `self._manual_heating_setpoint = 50.0`, `self._manual_pump_dump_active = False`.
   - Добавить методы `async_start_manual_heating(self, mode, setpoint)` и `async_stop_manual_heating(self)`. При изменении состояния вызывать событие шины `ems_manual_heating_updated`.
   - Добавить метод `_async_apply_manual_heating(self)`. Его логика:
     - `GAS`: байпас закрыт (`OFF`), газ в режиме `heat` с целевой `setpoint`, ТЭН выключен, насос выключен. Остановка при `t_gas >= setpoint`.
     - `GAS_PUMP`: байпас открыт (`ON`), газ в `heat` с целевой `setpoint`, ТЭН выключен, насос включен постоянно (`ON`). Остановка при `t_gas >= setpoint`.
     - `ELEC`: байпас открыт (`ON`), ТЭН включен (`ON`), газ выключен, насос выключен. Остановка при `t_elec >= setpoint`.
     - `ELEC_PUMP`: байпас открыт (`ON`), ТЭН включен (`ON`), газ выключен. Насос работает по схеме отсечки (включение при `temp >= setpoint - 5.0`, выключение при `temp < setpoint - 10.0`). Остановка при `t_elec >= setpoint`.
   - В `_async_temp_changed` при ручном нагреве вызывать `_async_apply_manual_heating`. При смене режима на `Auto` останавливать ручной нагрев.
2. **Сенсор (`sensor.py`)**:
   - В `EmsBoilerDpSensor` добавить прослушивание события `ems_manual_heating_updated` для принудительного обновления состояния.
   - В атрибуты `sensor.boiler_dp` экспортировать `manual_heating_active`, `manual_heating_mode` и `manual_heating_setpoint`.
3. **Сервисы (`services.py` и `services.yaml`)**:
   - Зарегистрировать сервисы `start_manual_heating` и `stop_manual_heating`.
4. **Интерфейс (`boiler-card.js`)**:
   - В режиме `Manual` рендерить дополнительную панель управления: выпадающий список выбора режима нагрева, ползунок сетпоинта (границы от `t_min` до `t_max_gas` / `t_max_elec`) и кнопку Старт/Стоп.
   - При перемещении ползунка вычислять прогноз расхода ресурсов на основе дельты температур и коэффициентов эффективности из `sensor.boiler_calibration` (с дефолтными фолбэками: 80.0 °C/м³ для газа и 8.6 °C/кВтч для электричества).
   - При выборе режима `GAS` отправлять команду на закрытие байпаса, при любом другом — на открытие.

### Skeptic
Критикую предложение Archi по следующим пунктам:
1. **Отсутствие валидации границ сетпоинта в бэкенде**: Если пользователь передаст некорректное значение через вызов сервиса (например, 95°C), контроллер будет непрерывно греть систему. В `start_manual_heating` на бэкенде должна быть жесткая проверка границ `[t_min, t_max]` для выбранного котла.
   *Archi's counter-measure*: Мы добавим валидацию в `async_start_manual_heating`: диапазон сетпоинта ограничивается снизу `t_min` (или 40°C), а сверху — `t_max_gas` для газа и `t_max_elec` для электричества, получаемыми из настроек. При выходе за границы будет выбрасываться `HomeAssistantError`.
2. **Избыточные вызовы сервисов байпаса при переключении режима в UI**: Пользователь может быстро перещелкивать режимы в выпадающем списке. Если на каждый клик UI будет слать команду переключения байпаса в HA, это приведет к перегрузке реле и шины.
   *Archi's counter-measure*: Мы будем вызывать переключение байпаса при изменении выбора в выпадающем списке только в том случае, если текущее состояние байпасного клапана в HA не совпадает с целевым.
3. **Безопасность при потере связи с температурным датчиком**: В методе `_async_apply_manual_heating` при отсутствии данных температуры (`t_gas is None` или `t_elec is None`) нагрев должен немедленно отключаться ради пожарной безопасности (Fail-Safe).

### Archi (Итерация 2)
Полностью согласен с замечаниями Skeptic:
1. Валидация сетпоинта добавлена в `async_start_manual_heating` с жесткой привязкой к максимумам греющих котлов.
2. В `_async_apply_manual_heating` добавлен Fail-Safe блок, который при потере связи с температурным датчиком мгновенно гасит нагрев и сбрасывает флаг активности.
3. В Lovelace-карточке вызов переключения байпаса при смене режима в выпадающем списке будет защищен сравнением с текущим состоянием клапана.

### Skeptic (Итерация 2)
Предложенные меры безопасности полностью закрывают риски перегрева при потере связи, а валидация на бэкенде исключает человеческие ошибки. Код готов к реализации.

### Заключение
Согласовано:
1. Реализовать ручной цикл нагрева в `BoilerController` с четырьмя режимами (`GAS`, `GAS_PUMP`, `ELEC`, `ELEC_PUMP`).
2. Добавить валидацию сетпоинтов и Fail-Safe отключение при пропадании данных.
3. Зарегистрировать сервисы `start_manual_heating` и `stop_manual_heating` в HA и yaml.
4. Экспортировать состояние ручного цикла в атрибуты `sensor.boiler_dp`.
5. Обновить карточку `boiler-card.js` для рендеринга новой интерактивной панели ручного управления с расчетом прогноза затрат по калибровочным данным.
"""

with open(debate_path, "r", encoding="utf-8") as f:
    content = f.read()

new_content = content.strip() + "\n" + debate_text

with open(debate_path, "w", encoding="utf-8") as f:
    f.write(new_content)

print("Debate appended successfully.")
