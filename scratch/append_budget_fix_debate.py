import os

debate_path = "e:/HA_INTEGRATIONS/EMS/DEBATE.md"

new_debate_entry = """
## [2026-05-28 14:23] Задача: Исправление расчета суммарного бюджета, использованной энергии и остатка лимита бойлера.

### Archi
Поскольку `slots` в планировщике содержат только текущие и будущие часы (начиная с текущего часа), расчет `total_pv_budget_today` в `run_boiler_dp` представляет собой *остаток* бюджета на сегодня (`R`), а не суммарный бюджет за полные сутки.
Если мы вычитаем `actual_boiler_today` (`U`) из остатка `R`, то:
1. Получаем неверное (заниженное до нуля) значение `remaining_pv_today` (`R - U`), что отключает оставшийся лимит.
2. Сумма бюджета (`2.4 кВтч`) расходится с использованным (`3.7 кВтч`) и остатком (`0.0 кВтч`).

Предлагаю изменить расчет в `stats`:
- `remaining_pv_today` = `total_pv_budget_today` (это и есть чистый остаток лимита на будущие часы `R`).
- `boiler_used_today` = `actual_boiler_today` (уже использовано `U`).
- `total_pv_budget_today` = `total_pv_budget_today + actual_boiler_today` (суммарный бюджет за весь день `R + U`).
Это сбалансирует равенство: Бюджет = Использовано + Осталось.

### Skeptic
Критический анализ выявляет 3 момента:
1. **Переполнение суммарного бюджета**: Если фактическое потребление бойлера по какой-то причине (например, ручной нагрев) превысило расчетный лимит, формула `R + U` корректно покажет большой бюджет, но нужно убедиться, что `remaining_pv_today` не зависит от этого и остается равен `R` (планируемому остатку). Предложенное решение Archi это учитывает, так как `remaining_pv_today = total_pv_budget_today`.
2. **Влияние на распределение слотов**: Внутренняя переменная `total_pv_budget_today` внутри функции `run_boiler_dp` используется для распределения свободных слотов. Изменение её значения *только* в возвращаемом словаре `stats` безопасно и не затронет логику оптимизации DP.
3. **Обнуление в конце дня**: В 23:00 остаток `R` будет стремиться к нулю, а `U` будет максимальным. Суммарный бюджет `R + U` корректно сойдется к фактическому суточному потреблению. Баг с двойным вычитанием полностью устраняется.

### Archi (итерация 2)
Все выводы Skeptic верны. Изменение затрагивает исключительно результирующий словарь `stats`, внутренняя логика распределения лимитов по часам остается нетронутой и работает на чистом остатке `R`.

### Skeptic (итерация 2)
Решение математически корректно, безопасно для алгоритма оптимизации и полностью устраняет путаницу в интерфейсе. Консенсус достигнут.

### Заключение
Реализуем исправление словаря `stats` в `boiler_dp_engine.py`:
- `"total_pv_budget_today": round(total_pv_budget_today + actual_boiler_today, 2)`
- `"boiler_used_today": round(actual_boiler_today, 2)`
- `"remaining_pv_today": round(total_pv_budget_today, 2)`
"""

if os.path.exists(debate_path):
    with open(debate_path, "a", encoding="utf-8") as f:
        f.write(new_debate_entry)
    print("Debate successfully appended.")
else:
    print("Error: DEBATE.md not found.")
