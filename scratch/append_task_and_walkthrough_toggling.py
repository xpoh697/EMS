import os

task_path = "task.md"
walkthrough_path = "walkthrough.md"

task_append = """
- [x] **Устранение chattering/toggling в авто-режиме**
  - [x] Внедрение фильтрации по фактическому изменению температуры в `_async_temp_changed` (`boiler_controller.py`).
  - [x] Корректировка дебаунсинга в `async_update_boiler_dp` до 3.0°C и 1 часа (`sensor.py`).
  - [x] Форсирование перерасчета (`force=True`) при изменениях тарифов, калибровок и настроек в `_async_state_changed_listener` (`sensor.py`).
  - [x] Сохранение ссылок на температурные датчики в инстансе `EmsBoilerDpSensor` в `async_added_to_hass`.
  - [x] Синхронизация и деплой на удаленный сервер.
"""

walkthrough_append = """

### 19. Устранение циклического переключения (Chattering/Toggling)
- **Причина chattering-эффекта**: 
  1. Планировщик DP запускал перерасчет на каждое событие изменения температуры. Из-за отсутствия температурного гистерезиса в планировщике, при температурах около уставки `t_min` (40.0°C) планы постоянно переключались между `GAS` и `IDLE`. Это вызывало chattering (тактование) и износ реле.
  2. Контроллер `boiler_controller.py` слушал события климата в `_async_temp_changed`. Любая команда управления (например, `set_hvac_mode` в `off`) вызывала повторный запуск цепочки проверок, создавая гонки и нецелевые мгновенные выключения.
- **Реализованные исправления**:
  - **Сенсор планирования (`sensor.py`)**: Сохранены ID температурных датчиков (`self._gas_sensor`, `self._elec_sensor`) в `async_added_to_hass`. В `_async_state_changed_listener` выставлен флаг `force = True` для всех сущностей (тарифы, калибровки, часы в UI), кроме температурных датчиков. Это обеспечивает мгновенный отклик интерфейса на настройки.
  - **Дебаунсинг планировщика (`sensor.py`)**: В `async_update_boiler_dp` дебаунсинг зажат до `3600.0` секунд (1 час) и `3.0`°C. План стабилен и не колеблется при микро-колебаниях температуры, но мгновенно перерасчитывается при резком разборе воды.
  - **Фильтрация событий в контроллере (`boiler_controller.py`)**: Метод `_async_temp_changed` переписан с использованием безопасного парсинга числовых значений `old_state` и `new_state`. Проверки срабатывают только в том случае, если действительно изменилась измеряемая датчиком температура, полностью игнорируя события смены HVAC-режима или целевой температуры ГВС.
- **Результат**: Циклическое переключение полностью устранено. Интеграция работает стабильно, перерасчеты расписания от изменения цен или настроек в интерфейсе происходят мгновенно, а физические реле защищены от частых переключений.
"""

if os.path.exists(task_path):
    with open(task_path, "a", encoding="utf-8") as f:
        f.write(task_append)
    print("task.md updated.")

if os.path.exists(walkthrough_path):
    with open(walkthrough_path, "a", encoding="utf-8") as f:
        f.write(walkthrough_append)
    print("walkthrough.md updated.")
