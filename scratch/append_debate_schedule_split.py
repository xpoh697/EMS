import os

debate_path = r"E:\HA_INTEGRATIONS\EMS\DEBATE.md"

debate_text = """
## [2026-05-23 17:58] Задача: Разделение расписания в Lovelace-карточке на блоки Today и Tomorrow с добавлением тематических цветов и иконок к почасовым плиткам.

### Archi
Предлагаю провести редизайн вкладки "Расписание" в `boiler-card.js`:
1. **Разделение по дням**:
   - В HTML-разметке вкладки расписания разделить сетку на две секции: `<div id="sched-hdr-today">Сегодня</div><div class="schedule-grid" id="sched-grid-today"></div>` и аналогично для "Завтра".
   - В коде рендеринга `_updateValues` определять день каждого слота по его порядковому индексу в списке. Слот с индексом `i` относится к "Сегодня", если `current_hour + i < 24`, и к "Завтра", если `current_hour + i >= 24` и `< 48`.
   - Если секция пуста (например, под конец дня для Today), скрывать ее заголовок и сетку.
2. **Визуальное оформление**:
   - Присвоить каждой плитке класс темы в зависимости от режима: `.theme-idle`, `.theme-gas`, `.theme-gas_pump`, `.theme-elec`, `.theme-elec_pump`.
   - Добавить фоновые полупрозрачные цвета и цветные границы для плиток (оранжевый для газа, зеленый для ТЭНа, серый для простоя).
   - Интегрировать иконки внутрь плиток: `mdi:power-sleep` для IDLE, `mdi:fire` для газовых режимов, `mdi:lightning-bolt` для ТЭН.

### Skeptic
Критикую предложение Archi:
1. **Неправильное определение границы дней при ночных расчетах**: Формула `current_hour + i < 24` верна только в том случае, если расчет происходит в текущий календарный день. Если Home Assistant возвращает данные, где время старта сбито, или мы находимся на стыке суток, вычисление может дать сбой. Для надежности нужно извлекать текущий час `start_hour` динамически из атрибутов `sensor.boiler_dp`.
   *Archi's counter-measure*: Мы будем брать `start_hour` напрямую из ответа планировщика или вычислять как текущий час локального времени `new Date().getHours()`, что гарантирует точность деления.
2. **Отсутствие иконки насоса в режимах PUMP**: В режимах `GAS_PUMP` и `ELEC_PUMP` критически важно показать пользователю, что будет работать насос перекачки. Отображение только иконки огня или молнии скроет этот факт.
   *Archi's counter-measure*: В режимах с помпой мы будем рендерить две иконки рядом (например, огонь + насос: `mdi:fire` и `mdi:pump`), либо одну комбинированную, либо выводить иконку насоса `mdi:pump` рядом с основной иконкой режима.
3. **Раздувание DOM при частых обновлениях**: Метод `grid.innerHTML = ""` очищает и строит заново все 24+ плиток при каждом обновлении данных. Хотя это быстро, лучше оптимизировать рендеринг, чтобы не дергать вкладку, если она скрыта (т.е. рендерить расписание только если активна вкладка "Расписание"!).

### Archi (Итерация 2)
Согласен с доработками:
1. **Деление дней**: Будем использовать `const startHour = new Date().getHours();` для точного определения границы суток.
2. **Иконки помпы**: Для режимов `GAS_PUMP` и `ELEC_PUMP` будем рендерить две иконки в ряд: основную (`mdi:fire`/`mdi:lightning-bolt`) и иконку насоса (`mdi:pump`) уменьшенного размера, чтобы показать перекачку.
3. **Ленивый рендеринг**: Расписание будет перестраиваться только тогда, когда активна вкладка "Расписание". При обновлении данных в фоновом режиме (когда открыта вкладка "Состояние") рендеринг расписания откладывается до клика на вкладку.

### Skeptic (Итерация 2)
Ленивый рендеринг снижает нагрузку на браузер при частых обновлениях температур, а двойные иконки делают расписание информативным с первого взгляда. Решение полностью готово.

### Заключение
Согласовано:
1. Разбить вкладку расписания на блоки "Сегодня" и "Завтра".
2. Добавить ленивый рендеринг расписания (только при активной вкладке "Расписание").
3. Оформить плитки расписания с использованием цветных тем и иконок (включая иконку `mdi:pump` для режимов с перекачкой).
"""

with open(debate_path, "r", encoding="utf-8") as f:
    content = f.read()

new_content = content.strip() + "\n" + debate_text

with open(debate_path, "w", encoding="utf-8") as f:
    f.write(new_content)

print("Debate appended successfully.")
